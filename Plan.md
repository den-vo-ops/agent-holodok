# Plan.md — Мастер-роадмап проекта Holodok Agent

> Рабочий трекер реализации проекта. Источник правды по требованиям — [spec.md](spec.md).
> Детальные пошаговые TDD-шаги ядра — [docs/superpowers/plans/2026-07-03-holodok-agent-core.md](docs/superpowers/plans/2026-07-03-holodok-agent-core.md).
> Этот файл — карта «куда идём» и «что уже сделано». Обновляется по ходу работы (см. §6).
>
> Дата создания: 2026-07-03 · Последнее обновление: 2026-07-04 (Фазы 1A и 1B реализованы; добавлены главное меню на reply-клавиатуре и онбординг/справка `/settov` + `/help`; LLM-провайдер сменён на Groq, 80 тестов зелёные)
>
> **⚠️ Смена LLM-провайдера (2026-07-04):** по решению владельца проект переведён с Claude/Anthropic на **Groq** (SDK `groq`, модель по умолчанию `llama-3.3-70b-versatile`). Переменные окружения теперь: `BOT_TOKEN`, `ADMIN_IDS`, `GROQ_API_KEY`, `GROQ_MODEL`, `DB_PATH`. Ниже по тексту исторические упоминания «Claude»/«Anthropic»/`ClaudeClient`/`ANTHROPIC_API_KEY` относятся к прежней реализации: в коде это теперь Groq-эквиваленты (`GroqClient`, `groq.*`, `GROQ_API_KEY`). Русские тексты ошибок из §3 не изменились (они провайдеро-нейтральны, говорят «ИИ»). spec.md как источник правды пока требует Claude — привести в соответствие через `discovery-interview`.

---

## 0. Легенда статусов и как читать план

| Значок | Статус | Значение |
|:---:|---|---|
| ⬜ | Не начато | Задача ещё не бралась в работу |
| 🟨 | В работе | Задача начата, но не завершена |
| ✅ | Готово | Задача завершена, тесты/критерии выполнены |
| ⛔ | Заблокировано | Нельзя начинать, пока не снят блокер (см. столбец «Блокеры») |
| ⏭️ | Отложено | Сознательно перенесено на будущее (условная фаза) |

**Как идти по плану:** сверху вниз по фазам. Внутри фазы — по порядку задач, соблюдая столбец «Зависит от». После каждой задачи и каждой фазы — обновлять статусы (§6).

---

## 1. Обзор фаз (порядок = приоритет)

| Фаза | Цель | Готовое состояние | Зависит от | Статус |
|---|---|---|---|:---:|
| **0. Предпосылки** | Снять неопределённости до начала кода (spec §9, §11) | Есть решение go/no-go по горячим лидам; известны параметры VPS; ясно, нужен ли антибот-прокси и укладывается ли в бюджет | — | ⬜ |
| **1A. Ядро агента** | Рабочий бот: доступ, обучение стилю, контент по кнопкам, память правил, черновики, метрики | Бот отвечает владельцу; онбординг→стиль; кнопки-сценарии; «запомни»; учёт «Опубликовал»; ежемесячный опрос; тесты зелёные | — | ✅ |
| **1B. Обработка ошибок LLM** | Ни один сбой ИИ не оставляет мастера без ответа | Любая ошибка LLM → понятный русский текст пользователю + полный трейсбек в лог | 1A (Task 5, Task 10) | ✅ |
| **1A+. Главное меню (reply)** | Постоянная reply-клавиатура из 5 кнопок как точка входа | Меню видно после `/start`; «Создать контент»/«Мои правила»/«Обучить стиль» работают, «Показать отчёт»/«Спросить о рынке» — заглушки до 1C/1D; тесты зелёные | 1A + 1B | ✅ |
| **1A++. Онбординг и справка** | Понятный первый запуск для нетехнического пользователя | Новому пользователю первый `/start` даёт 3 сообщения онбординга (что умеет / `/settov` / быстрый старт), затем меню; повторный `/start` — сразу меню; `/settov` грузит стиль; `/help` и кнопка «❓ Помощь» показывают инструкцию по каждой кнопке; заглушки объясняют «что будет и когда»; команды в меню Telegram; тесты зелёные | 1A + 1A+ | ✅ |
| **1C. Разведка Авито/Юла + отчёт** | Ежедневный сбор по конкурентам, еженедельный отчёт | Собираются цены/офферы; история «было→стало→дельта»; автодобавление конкурентов; недельный отчёт по расписанию; точечный запрос; отказоустойчивость сбора | 1A + 1B | ⬜ |
| **1D. Горячие лиды** | Ежедневный поиск прямых запросов клиентов в локальных чатах | Найденные лиды попадают в блок «Требует внимания» отчёта | Фаза 0 (go) + 1A + 1B | ⛔ |
| **2. Отзывы конкурентов** | Дайджест отзывов конкурентов | Мониторинг 2ГИС и Яндекс.Карт; что хвалят/ругают | 1C | ⏭️ |
| **3. Соцсети конкурентов (VK)** | Мониторинг страниц конкурентов | Реализуется только если gate-проверка подтвердит наличие активных страниц | Gate-проверка | ⏭️ |

**Границы MVP (Фаза 1):** А+В (Авито/Юла + контент) можно поставить и без модуля горячих лидов, если Фаза 0 даст «no-go» (spec §9.3).

---

## 2. Детализация по фазам

### Фаза 0 — Предпосылки / разблокировка

**Цель:** закрыть открытые вопросы spec §9 и §11 до того, как они станут блокерами.

| № | Задача | Артефакт | Зависит от | Статус |
|---|---|---|---|:---:|
| 0.1 | Найти реальные активные Telegram/VK-группы услуг Симферополя | Список групп + отметка активности | — | ⬜ |
| 0.2 | Определить техвозможность доступа (бот-участник vs user-аккаунт на чтение) | Решение по способу доступа | 0.1 | ⬜ |
| 0.3 | Решение go/no-go по модулю горячих лидов (Фаза 1D) | Запись решения здесь + снятие/сохранение ⛔ у 1D | 0.1, 0.2 | ⬜ |
| 0.4 | Уточнить параметры VPS (ОС, ресурсы) у владельца | Заметка для деплоя (Task 12) | — | ⬜ |
| 0.5 | Эмпирически оценить необходимость и бюджет антибот-прокси для Авито/карт | Вывод: укладывается в бюджет 2000–3000₽/мес или нет | — | ⬜ |

**Критерии готовности:** по 1D принято явное go/no-go; параметры VPS известны; по прокси есть эмпирический вывод.
**Блокеры:** нет (Фаза 0 сама разблокирует остальных).

---

### Фаза 1A — Ядро агента

**Цель:** рабочий скелет из [spec.md §5–8](spec.md). Пошаговые TDD-детали и готовый код — в [плане ядра](docs/superpowers/plans/2026-07-03-holodok-agent-core.md); здесь — трекер. **Код в этой таблице не дублируется** — см. соответствующий Task N.

| № | Задача (→ Task в плане ядра) | Файлы | Тесты | Зависит от | Статус |
|---|---|---|---|---|:---:|
| 1A-1 | Каркас пакета (Task 1) | `requirements.txt`, `pytest.ini`, `holodok_agent/**/__init__.py`, `.env.example`, `.gitignore` | — (pytest «no tests») | — | ✅ |
| 1A-2 | Загрузчик конфига (Task 2) | `holodok_agent/config.py` | `tests/test_config.py` | 1A-1 | ✅ |
| 1A-3 | Хранилище SQLite (Task 3) | `holodok_agent/db.py` | `tests/test_db.py` | 1A-1 | ✅ |
| 1A-4 | Whitelist владельца (Task 4) | `holodok_agent/bot/auth.py` | `tests/test_auth.py` | 1A-1 | ✅ |
| 1A-5 | Клиент Claude API (Task 5) | `holodok_agent/llm/client.py` | `tests/test_client.py` | 1A-1 | ✅ |
| 1A-6 | Анализ стиля по образцам (Task 6) | `holodok_agent/llm/style.py` | `tests/test_style.py` | 1A-5 | ✅ |
| 1A-7 | Извлечение правил «запомни, …» (Task 7) | `holodok_agent/rules.py` | `tests/test_rules.py` | 1A-1 | ✅ |
| 1A-8 | Генерация контента по сценариям (Task 8) | `holodok_agent/llm/content.py` | `tests/test_content.py` | 1A-5 | ✅ |
| 1A-9 | Клавиатуры и парсинг callback (Task 9) | `holodok_agent/bot/keyboards.py` | `tests/test_keyboards.py` | 1A-1 | ✅ |
| 1A-10 | Хендлеры: онбординг/сценарии/правила/публикация (Task 10) | `holodok_agent/bot/handlers.py`, `holodok_agent/bot/main.py` | `tests/test_handlers.py` + ручной smoke | 1A-3,4,5,6,7,8,9 | ✅ |
| 1A-11 | Планировщик ежемесячного опроса метрик (Task 11) | `holodok_agent/bot/scheduler.py`, `main.py` | `tests/test_scheduler.py` | 1A-10 | ✅ |
| 1A-12 | Деплой systemd + README (Task 12) | `deploy/holodok-agent.service`, `README.md` | ручная проверка на VPS | 1A-11, Фаза 0.4 | ✅ |

**Критерии готовности 1A:**
- [x] `python -m pytest` — всё зелёное (49 passed на 2026-07-03).
- [ ] Пройден ручной smoke-тест из плана ядра (Task 10 Step 6): `/start`→онбординг→`/done`→меню→«Дай идею»→«Опубликовал»→«запомни, …». — **ожидает владельца** (нужны реальные токены Telegram/Claude).
- [x] Whitelist работает в коде: фильтр `IsOwner` навешен на router (unit-тест `is_owner`); живой прогон «не-владелец не получает ответа» — в рамках ручного smoke.
- [x] Нет TODO/заглушек в новом коде.

> Код Фазы 1A завершён (49 тестов зелёные, точка входа собирается). «По-настоящему готово» — после Фазы 1B (обработка ошибок LLM), которая идёт следующей и дорабатывает `client.py`/`handlers.py`/`main.py`.

**Блокеры:** 1A-12 частично ждёт Фазу 0.4 (параметры VPS), но код бота от неё не зависит.

> ⚠️ **Важно:** Фаза 1B (обработка ошибок LLM) вплетается в 1A: 1B-2 дорабатывает `client.py` (после 1A-5), 1B-3 дорабатывает `handlers.py` (после 1A-10). Не считать 1A «готовым по-настоящему», пока не выполнена 1B — иначе любой сбой ИИ оставит мастера без ответа.

---

### Фаза 1B — Обработка ошибок LLM (НОВОЕ, сквозная)

**Цель:** централизованно перехватывать любые ошибки Claude API, показывать мастеру понятный русский текст и писать технические детали в лог. Ни один путь не должен приводить к молчанию бота.

**Архитектурное решение:** централизованно в клиенте. `ClaudeClient` ловит исключения Anthropic SDK → логирует трейсбек → бросает доменное `LLMError` с готовым русским `.user_message`. Хендлеры ловят `LLMError` и отправляют `.user_message`. Глобальный error-handler aiogram — сеть безопасности для всего непойманного.

| № | Задача | Файлы | Тесты | Зависит от | Статус |
|---|---|---|---|---|:---:|
| 1B-1 | Доменная ошибка + маппинг | `holodok_agent/llm/errors.py` | `tests/test_errors.py` | 1A-1 | ✅ |
| 1B-2 | Обёртка вызова в клиенте (лог + `raise LLMError`) | `holodok_agent/llm/client.py` | `tests/test_client.py` (доп.) | 1B-1, 1A-5 | ✅ |
| 1B-3 | Перехват `LLMError` в хендлерах → русский текст | `holodok_agent/bot/handlers.py` | `tests/test_handlers.py` (доп.) | 1B-2, 1A-10 | ✅ |
| 1B-4 | Глобальный error-handler aiogram (сеть безопасности) | `holodok_agent/bot/main.py` | ручной smoke (неверный ключ) | 1B-3, 1A-11 | ✅ |

**Детали задач:**

- **1B-1** `holodok_agent/llm/errors.py`:
  - `class LLMError(Exception)` с полем `.user_message: str` (готовый русский текст для пользователя). Технический текст — в аргументе исключения.
  - `to_llm_error(exc: Exception) -> LLMError` — маппит исключения Anthropic SDK (`RateLimitError`, `APITimeoutError`, `APIConnectionError`, `AuthenticationError`, `APIStatusError`, прочее) на русские сообщения из таблицы §3.
  - Тест: каждому типу исключения — ожидаемый русский текст; неизвестному исключению — общий fallback.

- **1B-2** доработка `holodok_agent/llm/client.py`:
  - `complete()` оборачивает `self._client.messages.create(...)` в `try/except anthropic.APIError as exc`.
  - В `except`: `logger.exception("Claude API call failed")` (полный трейсбек и детали разработчику), затем `raise to_llm_error(exc) from exc`.
  - Тест: мок SDK бросает `RateLimitError` → `complete()` бросает `LLMError` с текстом про «слишком много запросов»; факт логирования проверяется через `caplog`.

- **1B-3** доработка `holodok_agent/bot/handlers.py`:
  - Обернуть `analyze_style(...)` в `handle_onboarding_done` и `generate_content(...)` в `_generate_and_send` в `try/except LLMError as exc:` → `await message.answer(exc.user_message)` и корректно завершить (сбросить/сохранить состояние FSM так, чтобы мастер мог повторить).
  - Тест: мок бросает `LLMError` → пользователю уходит `.user_message`, а не трейсбек; черновик не помечается использованным при ошибке.

- **1B-4** глобальный error-handler в `holodok_agent/bot/main.py`:
  - Зарегистрировать `dp.errors.register(...)`: ловит ЛЮБОЕ непойманное исключение при обработке апдейта → `logger.exception(...)` + попытка отправить владельцу общий русский текст (fallback из §3).
  - Ручной smoke: временно подставить неверный `ANTHROPIC_API_KEY`, вызвать «Дай идею» → бот отвечает по-русски, а не молчит; в логах — трейсбек.

**Критерии готовности 1B:**
- [x] При любой ошибке LLM мастер получает понятный русский текст (не технический) — проверено поведенчески (маппинг + скрытие техтекста).
- [x] В логах — полный трейсбек с типом исключения и сценарием (`logger.exception` в клиенте и глобальном хендлере).
- [x] Нет пути «ошибка → молчание бота»: клиент → `LLMError` → хендлер, плюс глобальный error-handler aiogram как сеть безопасности. Живой smoke с неверным ключом — **ожидает владельца**.
- [x] Доп. тесты `test_errors.py` / расширения `test_client.py`, `test_handlers.py` — зелёные (итого 60 passed).

**Блокеры:** нет (идёт сразу за соответствующими задачами 1A).

---

### Фаза 1A+ — Главное меню на reply-клавиатуре

**Цель:** дать нетехническому мастеру постоянную точку входа — reply-клавиатуру из 5 кнопок, а не только inline-меню контента (spec §6 «Повседневное использование»).

**Детальный план:** [docs/superpowers/plans/2026-07-04-reply-menu.md](docs/superpowers/plans/2026-07-04-reply-menu.md) · дизайн: [docs/superpowers/specs/2026-07-04-reply-menu-design.md](docs/superpowers/specs/2026-07-04-reply-menu-design.md).

**Раскладка:** `[✍️ Создать контент] [📊 Показать отчёт]` / `[🔍 Спросить о рынке] [📝 Мои правила]` / `[⚙️ Обучить стиль]` (`resize_keyboard`, `is_persistent`).

| № | Задача | Файлы | Тесты | Зависит от | Статус |
|---|---|---|---|---|:---:|
| M-1 | `build_main_menu()` + константы подписей кнопок | `holodok_agent/bot/keyboards.py` | `tests/test_keyboards.py` (+1) | 1A-9 | ✅ |
| M-2 | Хендлеры кнопок + показ меню после `/start` и онбординга; сброс FSM при выходе из потока | `holodok_agent/bot/handlers.py` | `tests/test_handlers.py` (+5) | M-1, 1A-10 | ✅ |

**Поведение кнопок:**
- ✍️ Создать контент → открывает существующее inline-меню сценариев.
- 📝 Мои правила → список сохранённых правил (`db.get_hard_rules`) или подсказка «запомни, …», если пусто.
- ⚙️ Обучить стиль → перезапуск онбординга (перезапись профиля стиля).
- 📊 Показать отчёт / 🔍 Спросить о рынке → **заглушки** `REPORT_STUB_MESSAGE` / `MARKET_STUB_MESSAGE` (в `bot/messages.py`, обновлены в 1A++) до реализации Фаз 1C/1D.
- Нажатие любой кнопки меню сбрасывает незавершённый FSM-поток (аварийный выход) — иначе следующий текст мастера перехватил бы старый сценарий.

**Критерии готовности 1A+:**
- [x] `python -m pytest` — зелёное (67 passed).
- [x] Кнопки меню зарегистрированы раньше правил/state-хендлеров (аварийный выход из потока).
- [x] Нет TODO/заглушек в коде (заглушки-ответы кнопок — осознанная фича, не плейсхолдер).
- [ ] Ручной smoke с живым ботом: `/start` → 5 кнопок, живые/заглушки ведут себя как задумано — **ожидает владельца**.

**Блокеры:** нет. Заглушки заменяются реальными вызовами при 1C (отчёт/рынок).

---

### Фаза 1A++ — Онбординг и справка

**Цель:** чтобы новый человек (или сам владелец при первом запуске) сразу понимал, как работает бот и как им пользоваться (spec §6).

**Детальный план:** [docs/superpowers/plans/2026-07-04-onboarding-help.md](docs/superpowers/plans/2026-07-04-onboarding-help.md) · дизайн: [docs/superpowers/specs/2026-07-04-onboarding-help-design.md](docs/superpowers/specs/2026-07-04-onboarding-help-design.md).

| № | Задача | Файлы | Тесты | Зависит от | Статус |
|---|---|---|---|---|:---:|
| O-1 | Таблица `onboarded_users` + `has_onboarded`/`mark_onboarded` | `holodok_agent/db.py` | `tests/test_db.py` (+4) | 1A | ✅ |
| O-2 | Модуль текстов онбординга/справки | `holodok_agent/bot/messages.py` | `tests/test_messages.py` (+5) | — | ✅ |
| O-3 | Кнопка «❓ Помощь» в reply-меню | `holodok_agent/bot/keyboards.py` | `tests/test_keyboards.py` (+1) | M-1 | ✅ |
| O-4 | Онбординг-гейт в `/start`, команды `/settov` и `/help`, мини-подсказки, разнесённые заглушки | `holodok_agent/bot/handlers.py` | `tests/test_handlers.py` | O-1..O-3, M-2 | ✅ |
| O-5 | Регистрация `/start`, `/settov`, `/help` в меню команд Telegram | `holodok_agent/bot/main.py` | `tests/test_main.py` (+1) | O-4 | ✅ |

**Поведение:**
- Первый `/start` (нет в `onboarded_users`) → 3 сообщения онбординга → отметка + главное меню; в загрузку образцов **не** входим.
- Повторный `/start` → сразу меню.
- `/settov` (и кнопка «⚙️ Обучить стиль») → загрузка тон-оф-войс (образцы + `/done`).
- `/help` и кнопка «❓ Помощь» → инструкция с разбором каждой кнопки; сбрасывают активный FSM.
- Мини-подсказка перед действием каждой рабочей кнопки; заглушки «📊 Показать отчёт» / «🔍 Спросить о рынке» дают понятное «что будет и когда» (разные тексты).

**Критерии готовности 1A++:**
- [x] `python -m pytest` — зелёное (80 passed).
- [x] Онбординг показывается только новым пользователям (по `onboarded_users`).
- [ ] Ручной smoke с живым ботом (см. план, раздел «Ручной smoke-тест») — **ожидает владельца**.

**Блокеры:** нет.

---

### Фаза 1C — Разведка Авито/Юла + еженедельный отчёт

**Цель:** ежедневный сбор данных по конкурентам и еженедельный структурированный отчёт (spec §5А, §5Г).

> Детальный TDD-план создаётся отдельно через скилл `writing-plans` **перед** началом фазы (главный техриск — реальная разметка Авито/Юлы, spec §10.3).

| № | Задача (крупно) | Файлы-кандидаты | Зависит от | Статус |
|---|---|---|---|:---:|
| 1C-1 | Скрейпинг объявлений конкурентов (Авито/Юла) | `holodok_agent/scraping/avito.py`, `.../yula.py` | 1A, 1B | ⬜ |
| 1C-2 | Память о конкурентах + история цен «было→стало→дельта» | `holodok_agent/db.py` (новые таблицы) | 1A-3 | ⬜ |
| 1C-3 | Автопоиск и автодобавление новых конкурентов (без подтверждения) | `holodok_agent/scraping/discovery.py` | 1C-1,2 | ⬜ |
| 1C-4 | Сборка еженедельного отчёта (сводка / детали / «Требует внимания» / рекомендации / сбои сбора) | `holodok_agent/reports.py` | 1C-2 | ⬜ |
| 1C-5 | Планировщик: ежедневный сбор + еженедельный отчёт (всегда, даже пустой) | `holodok_agent/bot/scheduler.py` | 1A-11, 1C-1,4 | ⬜ |
| 1C-6 | Точечный запрос вне расписания (та же агрегация, что в отчёте) | `holodok_agent/reports.py`, `handlers.py` | 1C-4 | ⬜ |

**Зафиксированные компромиссы (spec §10) — соблюдать при реализации:**
- Уведомлять о **любом** изменении цены; порог — технически настраиваемый параметр, по умолчанию `0`.
- Отчёт присылается **всегда**, даже пустой («за неделю без изменений»).
- Новые конкуренты добавляются **без подтверждения**, видны в отчёте постфактум; мастер может попросить удалить.
- Сбои сбора — **строкой внутри ближайшего отчёта**, без отдельного алерта.

**Критерии готовности 1C:** ежедневный сбор идёт; недельный отчёт по расписанию строится по паттерну spec §5Г; история цен хранится; точечный запрос работает; сбои сбора отражаются строкой в отчёте; тесты зелёные.
**Блокеры:** нужен вывод Фазы 0.5 (прокси/бюджет).

---

### Фаза 1D — Горячие лиды

**Цель:** ежедневный поиск прямых запросов клиентов в локальных чатах (spec §5Б).

| № | Задача (крупно) | Файлы-кандидаты | Зависит от | Статус |
|---|---|---|---|:---:|
| 1D-1 | Доступ к подтверждённым группам (бот-участник или user-аккаунт) | `holodok_agent/leads/access.py` | Фаза 0 (go) | ⛔ |
| 1D-2 | Распознавание запросов «ищу мастера…» | `holodok_agent/leads/detect.py` | 1D-1, 1A, 1B | ⛔ |
| 1D-3 | Вывод лидов в блок «Требует внимания» отчёта | `holodok_agent/reports.py` | 1D-2, 1C-4 | ⛔ |

**Критерии готовности 1D:** найденные лиды ежедневно попадают в отчёт.
**Блокеры:** ⛔ **вся фаза заблокирована Фазой 0** — пока не подтверждены группы и способ доступа (spec §9). При «no-go» фаза сокращается/переносится, MVP ставится без неё.

---

### Фаза 2 — Отзывы конкурентов ⏭️

**Цель:** дайджест отзывов конкурентов на 2ГИС и Яндекс.Картах (выше приоритетом Google — ограничения Google в Крыму). Что хвалят/ругают.
**Зависит от:** 1C. **Детальный план:** через `writing-plans` перед стартом.
**Статус:** ⏭️ отложено до завершения Фазы 1.

### Фаза 3 — Соцсети конкурентов (VK) ⏭️

**Цель:** мониторинг страниц/групп конкурентов в VK.
**Gate-проверка:** реализуется **только если** на старте фазы подтвердится, что у местных мастеров есть активные страницы. Если нет — фаза не реализуется (а не откладывается бессрочно), spec §5 Фаза 3.
**Статус:** ⏭️ условно, после gate-проверки.

---

## 3. Русские сообщения об ошибках LLM (источник правды для текстов)

Тексты, которые видит мастер, и что при этом пишется в лог. Используются в `to_llm_error()` (1B-1). Тон — спокойный, без техножаргона, всегда с подсказкой «что делать».

| Тип ошибки (Anthropic SDK) | Причина | Текст пользователю (RU) | В лог |
|---|---|---|---|
| `RateLimitError` | Превышен лимит запросов | «Слишком много запросов к ИИ подряд. Подождите минуту и попробуйте снова.» | тип, `repr(exc)`, трейсбек, сценарий |
| `APITimeoutError` / `APIConnectionError` | Таймаут / нет связи с API | «Не удалось связаться с ИИ — похоже, проблемы с сетью. Попробуйте ещё раз через минуту.» | то же |
| `AuthenticationError` | Неверный/просроченный ключ | «Не получается обратиться к ИИ из-за настроек доступа. Я записал проблему в журнал — попробуйте позже.» | то же + пометка «проверить ANTHROPIC_API_KEY» |
| `APIStatusError` (5xx) | Сбой на стороне сервиса | «ИИ временно недоступен. Попробуйте ещё раз чуть позже.» | то же + код статуса |
| `BadRequestError` / 4xx (кроме auth) | Некорректный запрос/слишком длинный ввод | «Не получилось обработать запрос — возможно, текст слишком длинный. Попробуйте короче.» | то же |
| Прочее / неизвестное | Любая иная ошибка | «Извините, не получилось подготовить текст. Попробуйте ещё раз через пару минут.» | то же (общий fallback) |

**Правило логирования (везде):** `logger.exception(...)` с типом исключения, `repr(exc)`, полным трейсбеком и названием сценария/действия. Пользователю технический текст **не показывается никогда**.

---

## 4. Общая таблица прогресса (План | Факт | Статус | Блокеры)

| Задача | План (ожидаемое состояние) | Факт (что сделано) | Статус | Блокеры |
|---|---|---|:---:|---|
| 0.1 Группы для лидов | Найден список активных групп Симферополя | — | ⬜ | — |
| 0.2 Способ доступа | Выбран бот-участник или user-аккаунт | — | ⬜ | 0.1 |
| 0.3 Go/no-go по лидам | Явное решение зафиксировано | — | ⬜ | 0.1, 0.2 |
| 0.4 Параметры VPS | Известны ОС/ресурсы | — | ⬜ | — |
| 0.5 Прокси/бюджет | Эмпирический вывод по антиботу | — | ⬜ | — |
| 1A-1 Каркас | Пакет, requirements, pytest.ini, .env.example | `holodok_agent/`, `tests/`, requirements.txt, pytest.ini, .env.example, .gitignore (commit a16062d) | ✅ | — |
| 1A-2 Конфиг | `load_config()` + тесты | `config.py`, 3 теста (5abc828) | ✅ | 1A-1 |
| 1A-3 SQLite | стиль/правила/черновики/метрики + тесты | `db.py`, 7 тестов (1d6ae16) | ✅ | 1A-1 |
| 1A-4 Whitelist | `is_owner()` + тесты | `bot/auth.py`, 2 теста (f7ff078) | ✅ | 1A-1 |
| 1A-5 Claude-клиент | `ClaudeClient.complete()` + тесты | `llm/client.py`, 2 теста (1a94f69) | ✅ | 1A-1 |
| 1A-6 Анализ стиля | `analyze_style()` + тесты | `llm/style.py`, 5 тестов (3269396) | ✅ | 1A-5 |
| 1A-7 Правила | `extract_rule_from_message()` + тесты | `rules.py`, 5 тестов (020c79c) | ✅ | 1A-1 |
| 1A-8 Контент | `generate_content()` + тесты | `llm/content.py`, 8 тестов (782ef81) | ✅ | 1A-5 |
| 1A-9 Клавиатуры | меню/парсинг callback + тесты | `bot/keyboards.py`, 7 тестов (b667f6d) | ✅ | 1A-1 |
| 1A-10 Хендлеры | онбординг/сценарии/правила/публикация + тесты + smoke | `bot/handlers.py`, `bot/main.py`, 8 тестов; live-smoke ожидает владельца (a87b6a7) | ✅ | 1A-3..9 |
| 1A-11 Планировщик метрик | ежемесячный опрос + тесты | `bot/scheduler.py`, 2 теста (94acb32) | ✅ | 1A-10 |
| 1A-12 Деплой | systemd + README | `deploy/holodok-agent.service`, `README.md`; проверка на VPS ожидает владельца (13906ce) | ✅ | 1A-11, 0.4 |
| 1B-1 `errors.py` | `LLMError` + `to_llm_error()` + тесты | `llm/errors.py`, 8 тестов (b29fd23) | ✅ | 1A-1 |
| 1B-2 Клиент ловит | лог + `raise LLMError` + тесты | `client.py` try/except + лог, +1 тест (47d1f49) | ✅ | 1B-1, 1A-5 |
| 1B-3 Хендлеры ловят | русский текст пользователю + тесты | `handlers.py` перехват `LLMError`, +2 теста (105ca35) | ✅ | 1B-2, 1A-10 |
| 1B-4 Глобальный handler | сеть безопасности + smoke | `main.py` `dp.errors` + `FALLBACK_MESSAGE`; live-smoke ожидает владельца (68c6166) | ✅ | 1B-3, 1A-11 |
| M-1 Reply-клавиатура | `build_main_menu()` + 5 констант подписей | `bot/keyboards.py`, +1 тест (6ff4738) | ✅ | 1A-9 |
| M-2 Хендлеры меню | кнопки + меню после /start; сброс FSM при выходе | `bot/handlers.py`, +5 тестов (2700726, fix 5c5c02c); live-smoke ожидает владельца | ✅ | M-1, 1A-10 |
| 1C-1 Скрейпинг | Авито/Юла парсятся | — | ⬜ | 1A, 1B |
| 1C-2 История цен | «было→стало→дельта» хранится | — | ⬜ | 1A-3 |
| 1C-3 Автодобавление | новые конкуренты без подтверждения | — | ⬜ | 1C-1,2 |
| 1C-4 Отчёт | структура spec §5Г | — | ⬜ | 1C-2 |
| 1C-5 Планировщик сбора | ежедневно + недельный отчёт всегда | — | ⬜ | 1A-11, 1C-1,4 |
| 1C-6 Точечный запрос | та же агрегация, что в отчёте | — | ⬜ | 1C-4 |
| 1D-1 Доступ к группам | бот/аккаунт читает группы | — | ⛔ | Фаза 0 |
| 1D-2 Распознавание | запросы «ищу мастера…» | — | ⛔ | 1D-1, 1A, 1B |
| 1D-3 Лиды в отчёт | блок «Требует внимания» | — | ⛔ | 1D-2, 1C-4 |
| 2 Отзывы | 2ГИС/Яндекс дайджест | — | ⏭️ | 1C |
| 3 Соцсети VK | после gate-проверки | — | ⏭️ | gate |

---

## 5. Зависимости между задачами

**Фаза 1A (внутренний порядок из плана ядра):**
```
1A-1 (каркас)
 ├─ 1A-2 config
 ├─ 1A-3 db ─────────────┐
 ├─ 1A-4 auth ───────────┤
 ├─ 1A-5 client ─┬─ 1A-6 style ─┤
 │               └─ 1A-8 content┤
 ├─ 1A-7 rules ──────────┤
 └─ 1A-9 keyboards ──────┤
                         └─ 1A-10 handlers ─ 1A-11 scheduler ─ 1A-12 deploy
```

**Фаза 1B вплетается в 1A:**
- `1B-1` ← 1A-1
- `1B-2` ← 1B-1 + 1A-5 (дорабатывает client сразу после его создания)
- `1B-3` ← 1B-2 + 1A-10 (дорабатывает handlers)
- `1B-4` ← 1B-3 + 1A-11 (глобальный handler в main.py)

**Между фазами:**
- `1C` ← 1A + 1B (нужны db, клиент с обработкой ошибок, бот и планировщик)
- `1D` ← Фаза 0 (go) + 1A + 1B
- `2` ← 1C
- `3` ← gate-проверка

**Критический путь MVP:** 1A-1 → 1A-3/5 → 1A-6/8 → 1A-10 → 1B-2/3 → (1A-11, 1B-4) → 1A-12. Модули 1C/1D наращиваются поверх готового ядра.

---

## 6. Как обновлять этот файл (регламент автообновления)

Этот план — рабочий трекер, а не архив. Он обязан отражать реальность.

**После завершения каждой задачи:**
- Обновить её строку в **таблице фазы** (§2) и в **общей таблице прогресса** (§4): значок статуса + столбец «Факт» (что реально сделано, кратко).

**После завершения фазы:**
- Отметить чек-боксы «Критерии готовности» фазы.
- Пересмотреть блокеры зависимых фаз: если предпосылка снята — заменить ⛔ на ⬜ (например, Фаза 0 «go» разблокирует 1D).
- Обновить статус фазы в §1 и дату «Последнее обновление» в шапке.

**Связь с CLAUDE.md §9:**
Любая сессия, завершившая задачу или фазу, **обязана обновить Plan.md перед завершением работы** — не оставлять «на потом». Это то же правило самообновления, что действует для CLAUDE.md.

**При создании детального плана подсистемы** (1C, 1D, 2, 3) через скилл `writing-plans`:
- Разбить крупную задачу здесь на подзадачи со ссылкой на новый детальный план (как сделано для 1A → план ядра).

**Разовый шаг (вне plan-mode):** добавить в CLAUDE.md §2 и §7 упоминание `Plan.md` как рабочего трекера прогресса, дополняющего детальные планы в `docs/superpowers/plans/`.
